/*
 * ListTestimonial Messages
 *
 * This contains all the text for the ListTestimonial container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListTestimonial';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListTestimonial container!',
  },
});
