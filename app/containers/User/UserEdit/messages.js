/*
 * UserEdit Messages
 *
 * This contains all the text for the UserEdit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserEdit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserEdit container!',
  },
});
