/*
 * BlogEditor Messages
 *
 * This contains all the text for the BlogEditor container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BlogEditor';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BlogEditor container!',
  },
});
