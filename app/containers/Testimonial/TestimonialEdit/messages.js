/*
 * TestimonialEdit Messages
 *
 * This contains all the text for the TestimonialEdit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TestimonialEdit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TestimonialEdit container!',
  },
});
