/*
 * BlogCategory Messages
 *
 * This contains all the text for the BlogCategory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BlogCategory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BlogCategory container!',
  },
});
